dptech_fw1000_show_security_policy_hit_template_rules = [
    # ACTION 有值
    re.compile(
        r'^\s*'
        r'(?P<RULE_ID>\d+)\s+'
        r'(?P<RULE_NAME>\S.*?)\s*'
        r'(?P<ACTION>Pass|Drop|Senior action)\s+'
        r'(?P<STATE>enable|disable)\s+'
        r'(?P<HITTED>\d+)\s*$'
    ),
]