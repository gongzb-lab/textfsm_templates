Value Filldown NAME (\S+)
Value Required ENTRY (.+)

Start
  # 匹配 Domain-set 头部，例如: domain-set name test
  ^\s*domain-set name ${NAME}
  # 匹配: add domain test1.com
  ^\s*add domain ${ENTRY} -> Record
  # 遇到 # 号返回开始状态
  ^# -> Start